<% if notice %>
  <p><%= notice %></p>
<% end %>

<%= render partial: 'categories/categories_index', locals: {categories: @categories}%>
<%= render partial: 'items/items_index', locals: {items: @items}%>
